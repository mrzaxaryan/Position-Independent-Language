// Test sock_redirect with empty process path
// Should return -1 for empty path

var result = true;
var sock = sock_connect("3.223.36.72", 80);
if (sock >= 0) {
    var redirect_result = sock_redirect(sock, "");
    print("Result with empty path:", redirect_result);
    if (redirect_result == -1) { print("PASS"); }
    else {print("FAIL"); result = false; }
    sock_close(sock);
} else {
    // If we can't connect, skip the test
    print("FAIL");
    result = false;
}
