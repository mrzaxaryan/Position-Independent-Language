// Test socket connect

var result = true;
var sock = sock_connect("3.223.36.72", 80);
print("Socket handle:", sock);
if (sock >= 0) { 
    print("PASS"); 
    sock_close(sock); 
}
else 
{
    print("FAIL");
    result = false;
}