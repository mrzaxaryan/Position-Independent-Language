// Test multiple HTTP clients
var h1 = http_open("http://httpbin.org/get");
var h2 = http_open("http://httpbin.org/ip");
var result = true;
print("Handles:", h1, h2);
if (h1 >= 0 && h2 >= 0 && h1 != h2) { 
    print("PASS"); 
    http_close(h1); 
    http_close(h2); 
}
else {
     print("FAIL");
     result = false;
}

