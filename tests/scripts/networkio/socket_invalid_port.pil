// Test socket invalid port
var result = true;
var sock = sock_connect("127.0.0.1", 1);
if (sock < 0) { print("PASS"); }
else 
{
     print("FAIL");
     result = false;
}
if (sock >= 0) { sock_close(sock); }
